import { DistributionClass } from './distribution-class.model';

describe('DistributionClass', () => {
  it('should create an instance', () => {
    expect(new DistributionClass()).toBeTruthy();
  });
});
